name: flask
dependencies:
  # Step: Common
  - python=3.8
  - pip
  - setuptools
  - wheel
  - tqdm
  - pytest

  # Step: Web Common
  - urllib3
  - requests

  # Step: Flask
  - Flask
  - Jinja2
  - Werkzeug

  # Step: Additional Packages for Flask
  - flask-wtf
  - flask-bootstrap

  # Step: Mongo
  - pymongo