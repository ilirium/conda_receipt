name: web
channels:
  - default
  - conda-forge
dependencies:
  # Step: Common
  - python
  - pip
  - setuptools
  - wheel
  - tqdm
  - pytest

  # Step: Web Common
  - urllib3
  - requests

  # Step: AsyncIO
  - aiohttp
  - aiofiles

  # Step: Mongo
  - pymongo

  # Step: API
  - fastapi